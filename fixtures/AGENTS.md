# Fixtures

Redistributable, deterministic compatibility inputs only. Record upstream repository and commit in `README.md` whenever fixtures are copied. Never place client models, proprietary CSET/SMC data, vendor binaries, or secrets here.
